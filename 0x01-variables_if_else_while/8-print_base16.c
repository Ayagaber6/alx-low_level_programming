#include <stdio.h>
/**
 * main- entry point
 *
 * Description: program that prints all the numbers of base 16 in lowercase
 *
 * Return: 0 success
 */
int main(void)
{
	char n;

	for (n = 0 ; n >= 16 ; n++)
	{
		putchar (n);
	}
	putchar('n');
	return (0);
}
