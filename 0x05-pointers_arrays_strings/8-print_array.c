#include "main.h"
/**
 * main- entry point
 *
 * Description: print array integer elements
 * @a: pointer
 * @n: input element
 */
void print_array/*print elements of an array*/(int *a, int n)
{
	for (n = 0 ; n < *a.length ; i++)
	{
		if (*a[n] != '\0')
		{
			printf("%d, ", *a[n];
		}
		else
		{
			printf("%d", *a[n]);
		}
	}
	_putchar('\n');
}
